 

Idea for a production build of dashboard with docker and nginx.
Using a containerised web site on the main server for other apis in system.


docker build -t cs-dashboard:v1.0 -f Dockerfile.prod .

docker run -p3000:80 cs-dashboard:v1.0 




          <div className="column p-0" >
            <div className="ml-1 is-size-7">
              <label className="has-background-info-light pl-0">
                {activity.action.__typename === 'AcquireCS' && (
                  'Acquired from:'
                ) || (
                  'Source IP'
                )}
                <br /></label>
              {activity.action.sourceIp}
            </div>
            {activity.action.__typename === 'RequestCS' && (
            <div className="ml-1 is-size-7 mt-1">
              <label className="has-background-info-light pl-0">Requested to:<br /></label>
              {activity.parentIp}
            </div>)}
          </div>
